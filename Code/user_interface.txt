# This is a sketch of what I want the user's interface to look like.

from thecannon import train_model, estimate_labels
from read_aspcap import get_training_set, get_test_set 

training_set = get_stars(fits_files, training_label_names)
test_set = get_stars(fitsfiles)

coefficients = train_model(training_set)
newlabels = estimate_labels(coefficients, test_set)
